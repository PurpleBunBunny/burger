<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newDevoured="{{devoured}}">
		{{#if devoured}}Devoured!{{else}}Get Another!{{/if}}
	</button>

	{{!-- <button class="remove-burger" data-id="{{id}}">
		Delete!
	</button> --}}
</li>